

<!-- Compteur d'écoles sélectionnées affichant le nombre d'écoles -->
<div class="hat-notifs" >
  <span id="mon_comparateur_count" class="<%= "orange-counter" unless session[:selection].size == 0  %> ">
    <%= session[:selection].size if !session[:selection].nil? %>
  </span>
  <p>Comparez !</p>
</div>
