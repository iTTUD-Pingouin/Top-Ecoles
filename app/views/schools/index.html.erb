<div class="container">
  <h1> Toutes les Ecoles !</h1>

  <ul>
    <% @schools.each do |school| %>
      <%= link_to school_path(school) do  %>
        <li><%= school.name %></li>
      <% end %>
    <% end %>
  </ul>
</div>

